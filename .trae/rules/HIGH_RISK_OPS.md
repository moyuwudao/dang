---
alwaysApply: true
description: 高风险操作规范 - 需要特别小心和先问再做的操作
---

# HIGH_RISK_OPS.md - 高风险操作规范

> **构建规则** → 详见 [BUILD.md](BUILD.md)
> **操作决策色标** → 详见 [RED_LINES.md](RED_LINES.md) 中的颜色表

---

## 🟠 高风险操作（需要特别小心）

### 1. 破坏性操作

**需要特别小心**：
- ⚠️ 删除文件（尤其是 `lib/` 下的代码）
- ⚠️ `flutter clean` 或 `./gradlew clean`
- ⚠️ 删除 `build/` 目录
- ⚠️ 删除 `node_modules/` 或依赖
- ⚠️ 数据库迁移（涉及 schema 变更）
- ⚠️ `rsync --delete` 同步目录（会删除目标端不在源端的文件）
- ⚠️ `pm2 delete all`（会删除所有服务，慎用！）

**执行前**：
- 必须先说清楚要删什么
- 解释为什么需要删
- 等待确认

**特别提醒：`.env` 文件**
- ⚠️ 服务器上 `/home/admin/dang/server/.env` **必须存在**且包含 `REDIS_PASSWORD=Redis123456`
- ⚠️ 缺失会导致应用启动时 Redis 认证失败，进程反复崩溃（pm2 重启 1472 次）
- ⚠️ **不要**假设 `.env` 在 `/opt/...` 旧路径（已废弃）
- 完整规范 → [BUILD.md § .env 与 process.env 规范](BUILD.md#-env-与-processenv-规范部署必修)

---

### 2. 依赖变更

**需要特别小心**：
- ⚠️ 添加新的 pub 依赖
- ⚠️ 删除现有的 pub 依赖
- ⚠️ 升级依赖版本
- ⚠️ 更改 gradle 版本

**风险**：
- 可能引入不兼容
- 可能增加 APK 大小
- 可能影响性能

**执行前**：
- 说明加了什么/删了什么
- 说明为什么需要这个
- 确认后再操作

---

### 3. 构建产物

**需要特别小心**：
- ⚠️ 修改 `build/` 目录中的自动生成文件
- ⚠️ 删除构建缓存
- ⚠️ 修改 `android/app/build.gradle.kts` 中的签名配置

---

### 4. Android 签名

**绝对不能改**：
- ❌ 签名密钥路径
- ❌ 签名密钥别名
- ❌ 签名密钥密码
- ❌ key.properties 文件

**为什么重要**：
- 签名是 APK 的"身份证"
- 一旦签名变了，旧用户无法更新
- 签名丢失意味着无法发布更新

---

## 🟡 警告区域（做之前先问）

### 5. 架构和核心逻辑

**先问再做**：
- ⚠️ 修改 `lib/main.dart` 的结构
- ⚠️ 修改路由逻辑
- ⚠️ 修改状态管理核心逻辑
- ⚠️ 修改数据库 schema
- ⚠️ 修改 API 调用层

---

### 6. 其他人的代码

**先问再做**：
- ⚠️ 修改 `pubspec.yaml`
- ⚠️ 修改 `android/build.gradle.kts`
- ⚠️ 修改 `ios/` 下的配置
- ⚠️ 修改 Linux/macOS/Windows 平台的代码

---

### 7. 测试文件

**先问再做**：
- ⚠️ 修改 `test/` 下的测试
- ⚠️ 删除测试
- ⚠️ 添加新的测试依赖

---

## 操作决策速查

> **风险等级色标** → 详见 [RED_LINES.md](RED_LINES.md)

| 风险等级 | 操作类型 | 行动 |
|---------|---------|------|
| 🟠 高风险 | 破坏性操作、依赖变更、签名修改 | 先解释风险，等确认 |
| 🟡 警告 | 架构修改、配置修改、测试修改 | 先说明影响，等确认 |
| 🟢 自由 | UI 样式、文案、资源文件 | 直接做 |

---

## 更新记录

| 日期 | 更新内容 |
|-----|---------|
| 2026-05-21 | 从 RED_LINES.md 拆分，独立为 HIGH_RISK_OPS.md |
